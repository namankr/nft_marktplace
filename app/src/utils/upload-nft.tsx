import Container from '@mui/material/Container';    
import Button from '@mui/material/Button';

export default function UploadNft()
{
    return (
        <Container maxWidth="sm" >
            <Button variant="contained">Upload NFT Images to Arweave</Button>
        </Container>);
}